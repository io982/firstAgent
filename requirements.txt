requests>=2.31.0
pytest>=7.4.0
pytest-timeout>=2.2.0

# Векторная база. Хранилище Главы 4 по умолчанию; главам 1-3 не нужна.
chromadb>=0.4.22

# Стеммер для русского (Глава 6): «чтение» и «чтения» должны попадать
# в индекс одной основой, иначе поиск по словам считает их разными.
# Snowball, он же алгоритм Портера: https://github.com/snowballstem/snowball
# Необязательная — без неё поиск работает по точным формам слов.
snowballstemmer>=3.0

# Сборщик графов (Глава 8): тот же конвейер правки, собранный готовой
# библиотекой вместо chapter7/src/graph.py. НЕОБЯЗАТЕЛЬНАЯ и потому
# закомментирована: она тянет за собой 20 пакетов и 21 МБ на диске,
# а конвейер главы работает и без неё. Раскомментируйте, если хотите
# прогнать chapter8/src/pipeline_lg.py и сравнить сборки сами.
# langgraph>=1.2
