"""Сравнение моделей: кто отдаёт нативные tool_calls, а кто нет.

Аналог test_hybrid.py из Главы 7 — файл, который превращает утверждение
главы в проверяемый факт. Прогоняет один и тот же запрос с `tools`
по списку моделей и показывает, куда попал вызов.

СТАТУС: каркас Главы 8. Реализации нет.

Запуск (после реализации):
    python -m chapter8.test_formats
"""

# Модели, на которых снимался замер 20 августа 2026.
# Ожидаемый результат записан, чтобы расхождение было видно сразу.
KNOWN_MODELS = [
    ("qwen2_5coder3b_q5:latest", "text"),
    ("qwen2.5-coder:7b", "text"),
    ("llama3.1:8b", "native"),
]


def main():
    """Для каждой установленной модели из списка: определить формат и сравнить
    с ожидаемым. Отсутствующие модели пропускать, а не падать."""
    raise NotImplementedError("Глава 8: прогон по моделям")


if __name__ == "__main__":
    main()
