"""Агент с нативными tool_calls и откатом на текстовый формат (Глава 8).

Точка входа главы. Отличие от предыдущих: при старте агент выясняет
пробным запросом, каким форматом отвечает выбранная модель, и дальше
работает соответствующим способом.

СТАТУС: каркас Главы 8. Реализации нет.

Запуск (после реализации):
    python -m chapter8.agent
    $env:AGENT_MODEL = "llama3.1:8b"   # модель с нативным форматом
"""


def main():
    """REPL как в предыдущих главах, плюс определение формата при старте.

    Порядок:
    1. Проверить Ollama и модель (функции Главы 1).
    2. Подключить реестр плагинов (install_plugins Главы 4).
    3. Определить формат вызовов через probe_tool_format.
    4. Сообщить пользователю, какой формат выбран и почему.
    5. Цикл вопросов через ask_agent_dual.
    """
    raise NotImplementedError("Глава 8: точка входа")


if __name__ == "__main__":
    main()
